# List of default HLS conversion options taken from the sycl_vxx.py
set(HLS_OPT_OPTIONS
    --sroa-vxx-conservative
    --lower-mem-intr-to-llvm-type
    --lower-mem-intr-full-unroll
    --unroll-only-when-forced
)
